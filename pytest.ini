[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
# Note: simulate_*.py and seed_db.py are integration scripts that hit a live
# server / live Atlas at import time; they are intentionally NOT collected here
# (only test_*.py is). Run them manually against a running stack.
